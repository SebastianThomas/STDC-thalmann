[package]
name = "stdc_diving_algorithms"
version = "0.1.19"
edition = "2024"

[dependencies]
num = { version = "0.4.3", default-features = false, features = ["libm"] }

[features]
default = ["lin_exp"]
lin_exp = []
